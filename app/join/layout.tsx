import type { Metadata } from "next";
export const metadata: Metadata = { title: "加入我们 · Join us", description: "与星曜同谐一起定义下一代人机协作。" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
