import type { Metadata } from "next";
export const metadata: Metadata = { title: "团队 · Team", description: "认识星曜同谐的创始团队与研究方向。" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
